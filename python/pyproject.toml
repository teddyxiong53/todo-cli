[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "todo-cli"
version = "0.1.0"
description = "A multi-language TODO CLI research project"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
